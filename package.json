{
  "name": "nanocoop-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Offline-first, cryptographic community banking engine for low-resource environments",
  "author": "Franek Jemiolo",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "test:core": "cd packages/nanocoop-core && uv run pytest --cov=app --cov-report=term-missing --cov-fail-under=85 tests/",
    "test:teller": "npm run test --workspace=packages/nanocoop-teller",
    "test:sms": "npm run test --workspace=packages/nanocoop-sms-bridge",
    "build:web": "npm run build:web --workspace=packages/nanocoop-teller"
  },
  "devDependencies": {}
}
